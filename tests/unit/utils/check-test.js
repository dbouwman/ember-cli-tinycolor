import check from 'dummy/utils/check';
import { module, test } from 'qunit';

module('Unit | Utility | check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = check();
  assert.ok(result);
});
